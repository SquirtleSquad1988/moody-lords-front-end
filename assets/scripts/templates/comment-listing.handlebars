{{#each comments as |comment|}}
  <div id="blog-comment" data-id="{{comment.id}}">
      <p>{{comment.comment}}</p>
      {{#if comment.editable}}
        <span class="label label-danger del-comment" data-id="{{comment.id}}">Delete</span>
      {{/if}}
  </div>
{{/each}}
